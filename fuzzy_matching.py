"""Ce fichier contient le code lié aux fonctions de fuzzy matching."""


def levenshtein(mot1:str, mot2:str) -> int:
    """Prend en paramètres deux chaînes de caractères mot1 et mot2.
    Renvoie la distance de Levenshtein entre les deux chaînes de caractères.
    La distance de Levenshtein est un entier représentant le nombre minimum d'opérations nécessaires pour passer d'une chaîne à l'autre."""

    distance = 0 # Distance de Levenshtein (initialisée à 0)


    # Code à insérer ici


    return distance # Renvoyer la distance de Levenshtein trouvée